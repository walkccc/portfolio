<div id="work-exp" class="section__title">Work Experience</div>
<div class="section__container">
  {% for job in jobs %}
  <details class="group" open>
    <summary class="section__summary">
      <div class="section__summary__icons">
        {% include 'components/icons/arrow.njk' %}
        <i class="bx bxl-{{ job.svgName }} text-{{ job.color }} mr-2"></i>
        <!-- <div class="mr-2">{% include job.svgPath %}</div> -->
      </div>
      <h3 class="section__summary__title">
        {{ job.title }}
      </h3>
    </summary>
    <div class="section__details">
      <!-- <div class="section__job-title">{{ job.title }}</div> -->
      <div class="section__job-date">{{ job.date }}</div>
      <div class="section__job-location">{{ job.location }}</div>
      <ul>
        {% for description in job.descriptions %}
        <li class="list-disc">
          {{ description }}
        </li>
        {% endfor %}
      </ul>
    </div>
  </details>
  {% endfor %}
</div>
